server:
  port: 8763

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: spring-test
  cloud:
    config:
      name: service-hi
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: config-server
